import {
  Loader2,
  Mail,
  User,
  Lock,
  LogOut,
  Settings,
  ChevronDown,
} from "lucide-react"
import { FcGoogle } from "react-icons/fc"

export const Icons = {
  spinner: Loader2,
  google: FcGoogle,
  mail: Mail,
  user: User,
  lock: Lock,
  logout: LogOut,
  settings: Settings,
  chevronDown: ChevronDown,
}
